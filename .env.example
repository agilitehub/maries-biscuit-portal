# Copy to .env for local dev, or .env.production before `npm run deploy`.
# CRA embeds REACT_APP_* values at build time — set these before building for Firebase Hosting.

REACT_APP_AGILITE_API_URL=https://api.agilite.io
REACT_APP_AGILITE_API_KEY=your-agilite-api-key
